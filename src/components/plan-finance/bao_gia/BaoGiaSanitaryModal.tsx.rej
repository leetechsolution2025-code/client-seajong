@@ -2188,7 +2188,8 @@
         <div style={{ display: "flex", gap: 8, alignItems: "center" }}>
           {saveError && <span style={{ fontSize: 12, color: "#fca5a5", alignSelf: "center" }}><i className="bi bi-exclamation-circle" /> {saveError}</span>}
+          {isOutOfStock && <span style={{ fontSize: 12, color: "#fca5a5", alignSelf: "center" }}><i className="bi bi-exclamation-triangle" /> {isDirectOrder ? "Đơn có hàng hóa thiếu" : "Báo giá có hàng hóa thiếu"}</span>}
           {(!editData?.id || editData.trangThai === "draft") && !isDirectOrder && (
-            <button className="sanitary-modal-header-btn" onClick={() => handleSave("draft")} disabled={saving} style={{ padding: "6px 20px", background: "rgba(255,255,255,0.2)", color: "#fff", border: "1px solid rgba(255,255,255,0.4)", borderRadius: 8, cursor: saving ? "not-allowed" : "pointer", fontWeight: 600, fontSize: 13 }}>
+            <button className="sanitary-modal-header-btn" onClick={() => handleSave("draft")} disabled={saving || isOutOfStock} style={{ padding: "6px 20px", background: (saving || isOutOfStock) ? "rgba(255,255,255,0.4)" : "rgba(255,255,255,0.2)", color: "#fff", border: "1px solid rgba(255,255,255,0.4)", borderRadius: 8, cursor: (saving || isOutOfStock) ? "not-allowed" : "pointer", fontWeight: 600, fontSize: 13 }}>
               {saving ? "Đang lưu..." : "Lưu nháp"}
             </button>
           )}
